class AnimationConstants {
  static const Duration defaultTransition = Duration(milliseconds: 300);
  static const Duration harvestSequence = Duration(seconds: 2);
  
  // Animation durations and curves will be extracted here
}
